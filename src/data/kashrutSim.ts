/**
 * Removed per Rabbi Dvir's halachic review (comment 100):
 *   "כנל" — same as comment 98 on kasheringData.ts.
 *
 * The actual kashering tool in the app is a WebView embed of the Kosharot guide
 * (see app/tools/kashering.tsx). The native kashrut simulator that used to
 * live here was deemed redundant and removed.
 *
 * This file is retained as an empty stub for backwards compatibility — no
 * source files in the app currently import it.
 */

export {};
